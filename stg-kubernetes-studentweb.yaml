apiVersion: apps/v1
kind: Deployment
metadata:
  name: student-webapp-test-stg
spec:
  replicas: 5
  selector:
    matchLabels:
      app: student-webapp-test-stg
  template:
    metadata:
      labels:
        app: student-webapp-test-stg
    spec:
      containers:
      - name: student-webapp-test-stg
        image: harishtiru/student-webapp-test-stg:v25
        ports:
        - containerPort: 3000
        livenessProbe:
          httpGet:
            path: /
            port: 3000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /
            port: 3000
          initialDelaySeconds: 10
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: student-webapp-test-stg
spec:
  selector:
    app: student-webapp-test-stg
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30003 # Choose any available port number between 30000 and 32767
  type: NodePort
